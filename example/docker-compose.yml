services:
  webserv:
    build:
      context: ../
      dockerfile: example/Dockerfile
    ports:
      - "1998:1998"
      - "9999:9999"
    volumes:
      - ./public:/var/www/html:Z
      - ./config:/etc/webserv:Z
      - ./public/upload:/srv/upload:Z
